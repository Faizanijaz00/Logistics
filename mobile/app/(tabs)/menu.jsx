// Placeholder for the "Menu" tab. The tab press is intercepted in _layout to
// open the drawer instead of navigating here, so this never actually renders.
export default function MenuTab() {
  return null;
}
