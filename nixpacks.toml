[phases.setup]
nixPkgs = ['nodejs_20', 'python3']

[phases.install]
cmds = ['cd server && npm install']

[phases.build]
cmds = ['cd server && node setup.js']

[start]
cmd = 'cd server && node index.js'
